services:
    api:
        build: .
        ports:
            - 8000:8000
        depends_on:
            - postgres
        environment:
            - DATABASE_USERNAME=postgres
            - DATABASE_PASSWORD=aldrich1028
            - DATABASE_HOSTNAME=postgres
            - DATABASE_PORT=5432
            - DATABASE_NAME=fastapi
            - SECRET_KEY=09d25e094faa6ca2556c818166b7a9563b93f7099f6f0f4caa6cf63b88e8d3e7
            - ALGORITHM=HS256
            - ACCESS_TOKEN_EXPIRE_MINUTES=60

    postgres:
        image: postgres:16
        environment:
            - POSTGRES_PASSWORD=aldrich1028
            - POSTGRES_USER=postgres
            - POSTGRES_DB=fastapi
        ports:
            - 5433:5432
        volumes:
            - postgres_db:/var/lib/postgresql/data

volumes:
    postgres_db:
